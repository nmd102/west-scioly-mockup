/** @type {import('next').NextConfig} */
const nextConfig = {

    output: "export",

    basePath: "/west-scioly-mockup",

    images: {
        unoptimized: true,
    },
};

export default nextConfig;
